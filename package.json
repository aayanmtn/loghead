{
  "name": "loghead-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/build.js",
    "test": "echo 'Run test manually'",
    "deploy": "node scripts/deploy.js",
    "dev": "echo 'Run dev manually'"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "esbuild": "^0.27.0"
  }
}
